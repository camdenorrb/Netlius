package me.camdenorrb.netlius.net

/*
import java.io.ByteArrayInputStream
import java.io.DataInputStream

class PacketReader(private val client: Client) {

    val readAheadBytes = mutableListOf<Byte>()


    fun readByte(): Byte {

        DataInputStream(FileFil)
    }

    fun readBytes(size: Int): ByteArray {
        client.readByte()
        return ByteArray(size) {
            readByte()
        }
    }

    // Reads ahead and caches the bytes
    fun readBytesAhead(size: Int) {
        cli
        readAheadBytes += cli
    }


}
*/